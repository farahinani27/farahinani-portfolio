// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  // Update this to the real domain before deploying (used for sitemap/canonical).
  site: "https://farahinani.com",
});
